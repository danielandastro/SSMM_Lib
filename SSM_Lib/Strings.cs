using System;

namespace SSM_Lib
{
    public class Length
    {
        public string GetLength(string Input)
        {
            byte[] temp = Encoding.ASCII.GetBytes(input);

        }
    }
}
